# Archive

Superseded or stale design docs, kept for historical reference only. None of these
reflect the current implementation — see `../SLAD_AI_CONTENT_FACTORY.md` for the
live architecture reference.

- `V2_ARCHITECTURE_AND_DESIGN.md` — folded into the SLAD; already marked SUPERSEDED.
- `AI_LEARNING_CONTENT_FACTORY_PROPOSITION.md` — a rival Go-native stack proposal,
  explicitly rejected in favor of the Python/FastAPI/LangGraph stack actually built.
- `IMPLEMENTATION_STATUS.md` — a one-time, dated (2026-09-01) verification snapshot,
  not living guidance.
- `IMPLEMENTATION_SPECIFICATION.md` — the original detailed build spec; repo layout,
  agent list, and API surface are now materially out of date versus the real
  codebase (missing `course_agent`, `style_guide_agent`, the whole
  `backend/orchestration/` layer, and several routers/services added since).
- `LEARNER_HIGHLIGHT_ENGINE_SPEC.md` — a still-unbuilt feature spec, but scoped to
  ggcms's learner-facing frontend, not content-factory itself.
